# 🔧 Настройка переменных окружения на Vercel

## ❗ ВАЖНО: Без этих переменных проект НЕ будет работать!

Ошибка 500 означает, что переменные окружения не настроены в Vercel.

## 📋 Пошаговая инструкция:

### 1. Откройте настройки проекта на Vercel:
   - Перейдите на https://vercel.com
   - Выберите ваш проект `fitnes-centr`
   - Нажмите **Settings** (в верхнем меню)

### 2. Добавьте переменные окружения:
   - В левом меню выберите **Environment Variables**
   - Нажмите **Add New**

### 3. Добавьте первую переменную:
   - **Key (имя):** `NEXT_PUBLIC_SUPABASE_KEY`
   - **Value (значение):** `sb_publishable_Moc--u-yEntxxUxqOZbJfQ_ad2CQV0K`
   - **Environment:** Выберите все три:
     - ✅ Production
     - ✅ Preview  
     - ✅ Development
   - Нажмите **Save**

### 4. Добавьте вторую переменную:
   - **Key (имя):** `SUPABASE_SERVICE_ROLE_KEY`
   - **Value (значение):** `sb_secret_njDnWtVjU88Yu9Jy5BfB2A_S7617by5`
   - **Environment:** Выберите все три:
     - ✅ Production
     - ✅ Preview
     - ✅ Development
   - Нажмите **Save**

### 5. Перезапустите деплоймент:
   - Перейдите в **Deployments**
   - Найдите последний деплоймент
   - Нажмите на три точки (⋯) справа
   - Выберите **Redeploy**
   - Или просто сделайте новый коммит и пуш в GitHub

## ✅ Проверка:

После перезапуска проверьте:
1. Откройте ваш сайт на Vercel
2. Попробуйте войти с логином `Daniil` и паролем `123456`
3. Если всё работает - переменные настроены правильно!

## 🔍 Если всё ещё не работает:

1. Проверьте логи деплоймента в Vercel
2. Убедитесь, что переменные добавлены для всех окружений (Production, Preview, Development)
3. Убедитесь, что после добавления переменных вы перезапустили деплоймент

